import React from 'react'

function TableBodyComponent(props) {
    const { page, rowsPerPage, bodyData } = props;
    const isSelected  = (name) => {
         
    }
    return (
        <div>
            
        </div>
    )
}

export default TableBodyComponent
